---
# Deploy tylko homepage
- name: Deploy Hypersomnia Homepage
  hosts: web_servers
  gather_facts: yes

  tasks:
    - name: Homepage deployment
      import_tasks: tasks/homepage.yml
      tags: [homepage]
