{% include 'overall_header.twig' %}

<h1>Community Arenas</h1>
<div class="arenas">
{% for i in maps %}
  <div class="arena_item">
    <a href="{{ url }}arenas/{{ i.name }}">
      <img class="arena_miniature" src="{{ url }}arenas/{{ i.name }}/miniature.png" alt="{{ i.name }}">
      <span class="arena_name">{{ i.name }}</span>
      <span class="arena_author">by {{ i.author }}</span>
      <i class="arena_desc">{{ i.short_description }}</i>
    </a>
  </div>
{% endfor %}
</div>

{% include 'overall_footer.twig' %}
